@model LyaShop.Models.Flower

@{
    ViewData["Title"] = "הוספת פרח חדש - FloraLya";
}

<div class="container mt-5" dir="rtl">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg" style="border-radius: 20px; border: none;">
                <div class="card-header bg-dark text-white text-center" style="border-radius: 20px 20px 0 0;">
                    <h2 class="mb-0">🌷 הוספת פרח חדש לקטלוג</h2>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Create" enctype="multipart/form-data">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="Name" class="form-label fw-bold">שם הפרח</label>
                                    <input asp-for="Name" class="form-control" placeholder="למשל: ורד קלאסי" />
                                </div>
                                <div class="form-group mb-3">
                                    <label asp-for="Color" class="form-label fw-bold">שם הצבע (טקסט)</label>
                                    <input asp-for="Color" id="colorName" class="form-control" placeholder="למשל: ורוד עתיק" />
                                </div>
                                <div class="form-group mb-3">
                                    <label asp-for="Price" class="form-label fw-bold">מחיר</label>
                                    <input asp-for="Price" class="form-control" type="number" step="0.01" />
                                </div>
                            </div>

                            <div class="col-md-6 text-center">
                                <label class="form-label fw-bold d-block">תצוגה מקדימה</label>
                                <div class="image-preview-container mb-3 shadow-sm mx-auto" style="width: 200px; height: 200px; background: #f8f9fa; border-radius: 15px; display: flex; align-items: center; justify-content: center; overflow: hidden; border: 2px dashed #ddd;">
                                    <img id="imgPreview" src="#" alt="Preview" style="max-width: 100%; max-height: 100%; display: none;" />
                                    <i id="placeholderIcon" class="bi bi-camera text-muted" style="font-size: 3rem;"></i>
                                </div>

                                <div class="form-group mb-3">
                                    <input type="file" name="imageFile" id="imageInput" class="form-control" accept="image/*" />
                                </div>

                                <div class="form-group mb-3 mt-4">
                                    <label class="form-label fw-bold">כוון את הצבע (Hue):</label>
                                    <input asp-for="HueRotation" type="range" class="form-range" id="hueSlider" min="0" max="360" value="0">
                                    <div class="d-flex justify-content-between" style="direction: ltr;">
                                        <small>0°</small>
                                        <small id="hueValue">0°</small>
                                        <small>360°</small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="text-center mt-4">
                            <button type="submit" class="btn btn-success btn-lg px-5 shadow-sm" style="border-radius: 30px;">
                                <i class="bi bi-check-circle"></i> שמור פרח
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary btn-lg px-5 shadow-sm ms-2" style="border-radius: 30px;">ביטול</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // מאזין לאירוע שינוי קובץ (כאשר המשתמש בוחר תמונה מהמחשב)
    document.getElementById('imageInput').onchange = function (evt) {
        const [file] = this.files; // שליפת הקובץ הראשון שנבחר מהרשימה
        if (file) {
            const preview = document.getElementById('imgPreview');

            // יצירת כתובת URL זמנית מקומית בדפדפן המצביעה על הקובץ שהועלה
            preview.src = URL.createObjectURL(file);

            // הצגת אלמנט התמונה והסתרת אייקון המצלמה של ברירת המחדל
            preview.style.display = 'block';
            document.getElementById('placeholderIcon').style.display = 'none';
        }
    };

    // שליפת אלמנטים מה-DOM עבור לוגיקת שינוי הגוון (Hue Rotation)
    const slider = document.getElementById('hueSlider');
    const previewImg = document.getElementById('imgPreview');
    const hueDisplay = document.getElementById('hueValue');

    // האזנה לאירוע הגרירה של הסליידר (אירוע 'input' מופעל בזמן אמת תוך כדי הזזה)
    slider.addEventListener('input', function() {
        const val = this.value; // מקבל את הערך הנוכחי שבין 0 ל-360

        // החלת פילטר CSS מסוג hue-rotate על תמונת התצוגה המקדימה
        // פילטר זה מסובב את גלגל הצבעים של התמונה בהתאם למעלות שנבחרו
        previewImg.style.filter = `hue-rotate(${val}deg)`;

        // עדכון הטקסט המציג למשתמש את מספר המעלות הנוכחי
        hueDisplay.innerText = val + '°';
    });
</script>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">